package com.ainsln.rhythmicabc.ui.alphabet

enum class AlphabetTabs(val index: Int, val title: String) {
    Binary(index = 0, title = "Binary Letters"),
    Ternary(index = 1, title = "Ternary Letters")
}
